# 第四章——质子

> ***"万物负阴而抱阳‌，冲气以为和"***

## 引言

在第三章中，我们确立了电子的同向双吸积盘模型，并定量推导了其全部核心物理参数。电子的两盘自转方向相同，马格努斯力向内抽吸，形成局域低压力势差场，表现为负电荷。

本章我们将研究质子。质子与电子携带等量异号电荷，质量约为电子的 1836.15 倍。在流体动力学框架下，质子的正电荷特性要求其内部流体势场向外发散，这与电子的向内汇聚势场形成鲜明对比。

我们将沿用相同的方法论：从实验数据出发，结合经典受力分析与能量耗散最小化原则，逆向推导质子的内部拓扑结构，并验证其自洽性。

## 一、线索收集与质子结构假设

### 1. 实验数据

质子的基本实验参数如下：

- 质子质量： $m_p \approx 1.672621923 \times 10^{-27}\text{ kg}$
- 质子内能： $E_{\text{rot},p} \approx 938.272\text{ MeV}$
- 质子电荷： $+e \approx 1.602176634 \times 10^{-19}\text{ C}$
- 质子磁矩： $\mu_p \approx 1.410606797 \times 10^{-26}\text{ J/T}$

质子质量约为电子质量的 1836.15 倍，内能约为电子内能的 1836.15 倍。这意味着质子的内部结构与电子有显著差异。

### 2. 正电荷的流体本原

在第三章第十七节中，我们已经指出：同向双吸积盘结构因马格努斯力向内抽吸，形成负电荷的汇聚势场；而异向双吸积盘结构因非对称加速度向外发散，形成正电荷的高压喷射场。

因此，质子的内部结构必须是**异向旋转**的双吸积盘。

### 3. 基本结构假设

基于上述分析，我们提出以下质子结构假设：

1. **异向旋转**：盘 1 自转顺时针，盘 2 自转逆时针。两盘自转轴平行于公转轴，系统整体逆时针公转。
2. **半径不同**：盘 1 半径较大（ $R_1$ ），盘 2 半径较小（ $R_2$ ）。
3. **公转轴位置**：位于两盘相切处。

异向旋转导致两盘的马格努斯力方向相同（均指向盘 1 的外侧），而伯努利力为吸引力（指向对方）。这种非对称的受力布局是质子产生净向外力的根源，也是其正电荷特性的力学本原。

## 二、频率相等的推导

在第三章第八节中，我们通过能量耗散最小化原则证明了同向双吸积盘结构中自转角速度与公转角速度严格同步。对于质子异向双吸积盘模型，这一结论同样成立。

### 1. 能量耗散最小化的约束

根据第一章第六节的稳态系统与能量耗散最小化原则，任何能够在绝对空间中长期稳定存在的物理实体，其内部运动必须满足零耗散条件（ $\dot{E}_{\text{dis}} \to 0$ ）。

考虑质子异向双吸积盘系统：
- **盘 1（左侧）**：自转角速度 $\omega_{s,1}$（顺时针），公转角速度 $\omega_o$（逆时针）
- **盘 2（右侧）**：自转角速度 $\omega_{s,2}$（逆时针），公转角速度 $\omega_o$（逆时针）

### 2. 盘体内部的相位锁定

对于每个吸积盘，其内部流体在随系统公转的同时进行自转。若自转角速度与公转角速度存在失配，则盘体在公转一周的过程中，其内部各点的相对相位会发生周期性变化。

具体而言，设盘 1 的自转角速度与公转角速度之差为 $\Delta\omega_1 = |\omega_{s,1}| - |\omega_o|$。在公转周期 $T_o = 2\pi/\omega_o$ 内，盘 1 内部某一点相对于盘体参考系的相位偏移为：

$$\Delta\phi_1 = \Delta\omega_1 T_o = 2\pi \left( \frac{|\omega_{s,1}|}{\omega_o} - 1 \right)$$

若 $\Delta\omega_1 \neq 0$，则每公转一周，该点的相对相位都会发生累积性漂移。这种周期性相位变化会在盘体内部激发粘滞剪切应力，导致能量持续耗散至背景介质。

同理，盘 2 的相位偏移为：

$$\Delta\phi_2 = \Delta\omega_2 T_o = 2\pi \left( \frac{|\omega_{s,2}|}{\omega_o} - 1 \right)$$

### 3. 界面处的剪切耗散

除了盘体内部的耗散外，两盘在相切界面处的流体微元也存在相对运动。在切点附近，盘 1 边缘的切向流速与盘 2 边缘的切向流速必须匹配，否则会产生界面剪切应力。

异向旋转时，两盘在切点处的自转线速度方向相同（均指向盘 1 的外侧）。设盘 1 在切点处的自转线速度为 $v_{s,1} = \omega_{s,1} R_1$，盘 2 在切点处的自转线速度为 $v_{s,2} = \omega_{s,2} R_2$。若两者不匹配，界面处将产生持续的粘性剪切耗散。

### 4. 稳态条件：频率同步

质子作为寿命极长的基本粒子（实验下限 $> 10^{34}$ 年），必然处于长期稳定状态。根据能量耗散最小化原则：

1. **盘 1 内部**：必须满足 $|\omega_{s,1}| = \omega_o$，否则内部相位漂移导致非稳态
2. **盘 2 内部**：必须满足 $|\omega_{s,2}| = \omega_o$，否则内部相位漂移导致非稳态
3. **两盘界面**：必须满足 $v_{s,1} = v_{s,2}$ 与几何自洽，否则界面剪切导致非稳态

因此，两盘的自转角速度均必须与公转角频率严格同步：

$$|\omega_{s,1}| = |\omega_{s,2}| = \omega_o \equiv \omega$$

即：**盘 1、盘 2 的自转角频率与系统整体的公转频率三者严格同步**。

### 5. 物理图像

这一结论并非先验假设，而是稳态系统动力学约束的必然结果：

| 情形                                                         | 能量耗散                              | 稳定性               |
| :----------------------------------------------------------- | :------------------------------------ | :------------------- |
| $\omega_{s,1} \neq \omega_o$ 或 $\omega_{s,2} \neq \omega_o$ | 内部相位漂移与界面剪切 → 持续能量泄漏 | 非稳态，无法长期存在 |
| $\omega_{s,1} = \omega_o$ 且 $\omega_{s,2} = \omega_o$       | 无相对运动 → 零耗散                   | **稳态，可长期存在** |

因此，频率相等是质子作为稳定粒子在绝对空间中维持其拓扑结构与旋转运动状态不变的必要条件。该同步性并非人为构造的假设，而是能量耗散最小化原则在稳态极限下的必然推论。

后续受力分析与定量计算均在这一频率同步的基础上展开。

## 三、受力分析

### 1. 力的方向判定

设盘 1 在左侧，盘 2 在右侧，公转轴位于两盘切点。系统整体逆时针公转。由第二节已证，两盘自转角速度与公转角速度相等（ $|\omega_{s,1}| = |\omega_{s,2}| = \omega_o \equiv \omega$ ）。

**盘 1（顺时针自转）**：
- 向心力：向右（指向公转轴）
- 马格努斯力：向左（远离公转轴，指向盘 1 的外侧）
- 伯努利力：向右（异向旋转时为吸引力，指向盘 2）

**盘 2（逆时针自转）**：
- 向心力：向左（指向公转轴）
- 马格努斯力：向左（指向公转轴）
- 伯努利力：向左（吸引力，指向盘 1）

注意：盘 1 与盘 2 的马格努斯力虽然均指向左侧，但相对于各自的公转半径，盘 1 的马格努斯力向外（远离公转轴），盘 2 的马格努斯力向内（指向公转轴）。这是因为两盘自转方向相反，与公转的相对关系不同。

### 2. 力的表达式（角速度形式）

沿用第三章第八节的力的表达式，用角速度表示。由第二节频率同步条件， $v_s = \omega R$， $v_o = \omega R$。

**盘 1**：

$$F_{M1} = k_{M,n} \rho_{0,n} \omega^2 R_1^3 h_1$$

$$F_{B1} = 4 \rho_{d1} \omega^2 R_1^3 h_1$$

$$F_{C1} = \pi \rho_{d1} \omega^2 R_1^3 h_1$$

**盘 2**：

$$F_{M2} = k_{M,n} \rho_{0,n} \omega^2 R_2^3 h_2$$

$$F_{B2} = 4 \rho_{d2} \omega^2 R_2^3 h_2$$

$$F_{C2} = \pi \rho_{d2} \omega^2 R_2^3 h_2$$

其中 $k_{M,n}$ 为近场马格努斯系数， $\rho_{0,n}$ 为近场背景介质密度。

### 3. 稳定性约束

对于盘 1，取向右为正方向（远离公转轴），净向外力为：

$$F_{net1} = F_{B1} - F_{M1} - F_{C1}$$

对于盘 2，取向左为正方向（远离公转轴），净向外力为：

$$F_{net2} = F_{B2} + F_{M2} - F_{C2}$$

两盘的质量分别为：

$$m_1 = \pi \rho_{d1} R_1^2 h_1, \quad m_2 = \pi \rho_{d2} R_2^2 h_2$$

稳定性约束要求两盘的净向外加速度相等，即：

$$\frac{F_{net1}}{m_1} = \frac{F_{net2}}{m_2}$$

代入具体表达式并约去公共因子 $\omega^2$：

$$\frac{(4\rho_{d1} - k_{M,n}\rho_{0,n} - \pi\rho_{d1}) R_1}{\pi\rho_{d1}} = \frac{(4\rho_{d2} + k_{M,n}\rho_{0,n} - \pi\rho_{d2}) R_2}{\pi\rho_{d2}}$$

整理分子，定义近场耦合强度与单盘密度之比 $A_1 = \frac{k_{M,n}\rho_{0,n}}{\rho_{d1}}$， $A_2 = \frac{k_{M,n}\rho_{0,n}}{\rho_{d2}}$，代入上式约去 $\pi$：

$$(4 - \pi - A_1) R_1 = (4 - \pi + A_2) R_2$$

此式即为质子异向双吸积盘模型的核心稳定性方程。其中 $A_1$ 与 $A_2$ 分别表征两盘的近场耦合强度与密度之比，在密度尚未确定相等之前，两者作为独立变量保留。

## 四、近场耦合参数与几何求解

### 1. 质子固有频率的确定

质子与电子可以稳定结合形成氢原子。根据第一章第六节的能量耗散最小化原则，若质子与电子的固有频率存在失配，则在原子尺度上会产生持续的周期性相位漂移，导致粘滞剪切耗散。只有当两者频率同步时，系统才能维持长期稳态。

因此，质子的固有频率与电子相同：

$$\nu_p = \nu_e = \nu$$

### 2. 电荷等量约束与体积关系

质子与电子携带等量电荷，即 $e_p = e_e = e$。由第三章第十七节的电荷对应关系：

$$e = \kappa_Q \cdot \nu V$$

其中 $V$ 为粒子总体积。对于电子， $V_e = 2\pi k R_s^3$。对于质子， $V_p = \pi k (R_1^3 + R_2^3)$。

将 $\nu_p = \nu_e = \nu$ 和 $e_p = e_e = e$ 代入电荷对应关系：

$$\kappa_Q \cdot \nu V_p = \kappa_Q \cdot \nu V_e \implies V_p = V_e$$

即：

$$\pi k (R_1^3 + R_2^3) = 2\pi k R_s^3 \implies R_1^3 + R_2^3 = 2 R_s^3$$

### 3. 近场耦合强度比值

近场耦合强度 $k_{M,n}\rho_{0,n}$ 为系统固有参数，不随粒子种类变化。电子的近场耦合强度比值为 $A_e = k_{M,n}\rho_{0,n}/\rho_{d,e} = 4 + \pi$。

质子平均密度 $\rho_d = m_p/V_p = m_p/V_e$，因此：

$$\frac{\rho_d}{\rho_{d,e}} = \frac{m_p}{m_e} \approx 1836.15$$

质子的近场耦合强度比值为：

$$A_p = \frac{k_{M,n}\rho_{0,n}}{\rho_d} = \frac{k_{M,n}\rho_{0,n}}{\rho_{d,e}} \cdot \frac{\rho_{d,e}}{\rho_d} = \frac{4 + \pi}{1836.15} \approx 0.003889$$

### 4. 质量与密度的缩放关系

由第一章的结论，频率相等的系统中，质量与半径平方成反比，密度与半径五次方成反比。因此：

$$\frac{m_1}{m_2} = \left(\frac{R_2}{R_1}\right)^2 = \frac{1}{x^2}, \quad \frac{\rho_{d1}}{\rho_{d2}} = \left(\frac{R_2}{R_1}\right)^5 = \frac{1}{x^5}$$

其中 $x = R_1/R_2$。

由质量守恒 $m_1 + m_2 = m_p$：

$$m_1 = \frac{m_p}{x^2 + 1}, \quad m_2 = \frac{m_p x^2}{x^2 + 1}$$

### 5. $A_1$、$A_2$ 与 $A_p$ 的关系

由定义 $A_i = k_{M,n}\rho_{0,n}/\rho_{di}$ 和 $\rho_d = m_p/(V_1 + V_2)$：

$$\frac{A_1}{A_p} = \frac{\rho_d}{\rho_{d1}} = \frac{m_p}{m_1} \cdot \frac{V_1}{V_1 + V_2} = (x^2 + 1) \cdot \frac{x^3}{x^3 + 1}$$

$$\frac{A_2}{A_p} = \frac{\rho_d}{\rho_{d2}} = \frac{m_p}{m_2} \cdot \frac{V_2}{V_1 + V_2} = \frac{x^2 + 1}{x^2} \cdot \frac{1}{x^3 + 1}$$

即：

$$A_1 = A_p \cdot \frac{x^3(x^2 + 1)}{x^3 + 1}, \quad A_2 = A_p \cdot \frac{x^2 + 1}{x^2(x^3 + 1)}$$

验证比值：

$$\frac{A_1}{A_2} = x^5 = \frac{\rho_{d2}}{\rho_{d1}}$$

### 6. 稳定性方程的数值求解

稳定性方程为：

$$(4 - A_1 - \pi)R_1 = (4 + A_2 - \pi)R_2$$

两边除以 $R_2$，代入 $A_1$、$A_2$ 的表达式。引入中间变量 $C = A_p \cdot \frac{x^2 + 1}{x^3 + 1}$，则 $A_1 = C x^3$，$A_2 = C/x^2$。代入并整理：

$$(4 - C x^3 - \pi)x = 4 + \frac{C}{x^2} - \pi$$

$$(4 - \pi)(x - 1) = C\left(x^4 + \frac{1}{x^2}\right) = A_p \cdot \frac{x^2 + 1}{x^3 + 1} \cdot \frac{x^6 + 1}{x^2}$$

由于 $A_p \approx 0.00389 \ll 1$，可预期 $x$ 非常接近 1。令 $x = 1 + \delta$（$\delta \ll 1$），零阶近似下：

$$(4 - \pi)\delta \approx A_p \cdot \frac{2}{2} \cdot 2 = 2A_p \implies \delta \approx \frac{2A_p}{4 - \pi} \approx 0.00906$$

通过牛顿迭代法数值求解，得到精确解：

$$x = \frac{R_1}{R_2} = 1.009106$$

**Python 验证代码**：读者可用以下脚本自行验证数值求解过程：

```python
import math

# 近场耦合强度比值
A_p = (4 + math.pi) / 1836.15

# 稳定性方程 f(x) = 0
def f(x):
    C = A_p * (x**2 + 1) / (x**3 + 1)
    lhs = (4 - C * x**3 - math.pi) * x
    rhs = 4 + C / x**2 - math.pi
    return lhs - rhs

# 牛顿迭代求解
x = 1.009  # 初始值（近似解）
for i in range(20):
    dx = 1e-10
    df = (f(x + dx) - f(x - dx)) / (2 * dx)
    x = x - f(x) / df

print(f"R1/R2 = {x:.10f}")
print(f"f(x)  = {f(x):.3e}")

# 验证稳定性方程
C = A_p * (x**2 + 1) / (x**3 + 1)
A1 = C * x**3
A2 = C / x**2
lhs = (4 - A1 - math.pi) * x
rhs = 4 + A2 - math.pi
print(f"LHS/RHS = {lhs/rhs:.15f}")
```

### 7. 几何参数

由体积约束 $R_1^3 + R_2^3 = 2R_s^3$ 和 $R_1 = x R_2$：

$$R_2 = \left(\frac{2 R_s^3}{x^3 + 1}\right)^{1/3} \approx 3.844 \times 10^{-13}\text{ m}$$

$$R_1 = x R_2 \approx 3.879 \times 10^{-13}\text{ m}$$

两盘半径均接近电子特征半径 $R_s \approx 3.862 \times 10^{-13}\text{ m}$。

### 8. 质量与密度

$$m_1 = \frac{m_p}{x^2 + 1} \approx 8.287 \times 10^{-28}\text{ kg}$$

$$m_2 = \frac{m_p x^2}{x^2 + 1} \approx 8.439 \times 10^{-28}\text{ kg}$$

$$\rho_{d1} = \frac{m_1}{V_1} \approx 1.492 \times 10^{10}\text{ kg/m}^3$$

$$\rho_{d2} = \frac{m_2}{V_2} \approx 1.562 \times 10^{10}\text{ kg/m}^3$$

验证： $\rho_{d1}/\rho_{d2} \approx 0.9557$， $(R_2/R_1)^5 \approx 0.9557$，两者一致。

### 9. 能量自洽性验证

自转动能与公转动能之和为：

$$E_{\text{self}} + E_{\text{orbital}} = (m_1 R_1^2 + m_2 R_2^2)\omega^2 \approx 1.50312 \times 10^{-10}\text{ J}$$

质子总内能为：

$$E_{\text{rot},p} = m_p c^2 \approx 1.50328 \times 10^{-10}\text{ J}$$

差值为进动动能：

$$E_{\text{precession}} = E_{\text{rot},p} - (E_{\text{self}} + E_{\text{orbital}}) \approx 1.54 \times 10^{-14}\text{ J}$$

进动动能仅占总内能的 $0.010\%$，说明质子的自转与公转运动占据了绝大部分能量，进动效应极小。

### 10. 小结

本节基于 $\rho \propto 1/R^5$ 的缩放关系，完成了质子异向双吸积盘模型的几何参数求解。核心结论：

1. 稳定性方程的精确解给出 $R_1/R_2 = 1.009106$，两盘高度对称。
2. 质子密度 $\rho_d \approx 1.527 \times 10^{10}\text{ kg/m}^3$，约为电子密度的 1836.15 倍。
3. 两盘密度不等： $\rho_{d1} \approx 1.492 \times 10^{10}\text{ kg/m}^3$， $\rho_{d2} \approx 1.562 \times 10^{10}\text{ kg/m}^3$，比值严格满足 $(R_2/R_1)^5$。
4. 自转+公转动能占总内能的 99.99%，进动动能仅占 0.01%，模型自洽。

## 五、几何参数求解与能量自洽性验证

第四节已求出质子的核心几何与物理参数。本节直接引用这些结果，补充转动惯量与角动量的计算，并完成力平衡与能量守恒的数值验证。

### 1. 几何参数汇总

由第四节得到的半径比 $x = R_1/R_2 = 1.009106$，结合体积约束 $R_1^3 + R_2^3 = 2 R_s^3$，解得：

$$R_1 \approx 3.879 \times 10^{-13}\text{ m}, \quad R_2 \approx 3.844 \times 10^{-13}\text{ m}$$

两盘厚度（取厚轴比 $k = 0.302822$）：

$$h_1 = k R_1 \approx 1.175 \times 10^{-13}\text{ m}, \quad h_2 = k R_2 \approx 1.164 \times 10^{-13}\text{ m}$$

单盘体积：

$$V_1 = \pi R_1^2 h_1 \approx 5.553 \times 10^{-38}\text{ m}^3, \quad V_2 = \pi R_2^2 h_2 \approx 5.404 \times 10^{-38}\text{ m}^3$$

总体积 $V_p = V_1 + V_2 \approx 1.096 \times 10^{-37}\text{ m}^3$，与电子体积 $V_e$ 完全一致，验证了电荷等量约束的自洽性。

### 2. 质量分布

由第一章的缩放关系（频率相等时质量与半径平方成反比）：

$$\frac{m_1}{m_2} = \frac{R_2^2}{R_1^2} = \frac{1}{x^2} \approx 0.9820$$

结合 $m_1 + m_2 = m_p$，解得：

$$m_1 \approx 8.287 \times 10^{-28}\text{ kg}, \quad m_2 \approx 8.439 \times 10^{-28}\text{ kg}$$

两盘质量接近但不完全相等，差异源于半径的微小不对称。盘 2 半径较小但质量较大，这是因为密度与半径的五次方成反比（ $\rho_{d1}/\rho_{d2} = R_2^5/R_1^5 \approx 0.9557$ ），盘 2 的密度更高。

两盘的平均密度为：

$$\rho_{d,\text{avg}} = \frac{m_p}{V_p} \approx 1.527 \times 10^{10}\text{ kg/m}^3$$

该密度约为电子密度的 1836.15 倍，与质量比一致。

### 3. 转动惯量与角动量

吸积盘的质量主要集中在边缘，转动惯量取 $I = m R^2$。

自转和公转的转动半径分别是盘心到自转轴和公转轴（切点）的距离。对于盘 1，这两个距离都是 $R_1$；对于盘 2，这两个距离都是 $R_2$。因此：

$$I_{s1} = m_1 R_1^2 \approx 1.247 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$
$$I_{s2} = m_2 R_2^2 \approx 1.247 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$
$$I_{o1} = m_1 R_1^2 = I_{s1}, \quad I_{o2} = m_2 R_2^2 = I_{s2}$$

自转与公转的转动惯量完全相等。总转动惯量为：

$$I_{\text{self}} = I_{s1} + I_{s2} \approx 2.494 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$
$$I_{\text{orbit}} = I_{o1} + I_{o2} = I_{\text{self}}$$

角速度为：

$$\omega = \omega_e = \frac{m_e c^2}{\hbar} \approx 7.763 \times 10^{20}\text{ rad/s}$$

各角动量分量：

$$L_{s1} = I_{s1} \omega \approx 9.681 \times 10^{-32}\text{ J}\cdot\text{s}$$
$$L_{s2} = I_{s2} \omega \approx 9.681 \times 10^{-32}\text{ J}\cdot\text{s}$$
$$L_{o1} = I_{o1} \omega = L_{s1}, \quad L_{o2} = I_{o2} \omega = L_{s2}$$

自转角动量与公转角动量之和为：

$$L_{\text{rot}} = L_{\text{self}} + L_{\text{orbit}} \approx 3.872 \times 10^{-31}\text{ J}\cdot\text{s} \approx 3671.93\hbar$$

由第二章的公式 $E_{\text{rot}} = \pi L \nu$，质子的总角动量为：

$$L_{\text{total}} = \frac{E_{\text{tot}}}{\pi \nu} = \frac{m_p c^2}{\pi \nu} \approx 3.873 \times 10^{-31}\text{ J}\cdot\text{s} \approx 3672.31\hbar$$

质子的总角动量约为 $3672\hbar$，远大于电子的 $2\hbar$，与质子内能远大于电子内能（约 1836 倍）的物理图像一致。

两者的差值为进动角动量：

$$L_{\text{prec}} = L_{\text{total}} - L_{\text{rot}} \approx 3.98 \times 10^{-35}\text{ J}\cdot\text{s} \approx 0.38\hbar$$

进动角动量仅占总角动量的约 $0.01\%$，与进动动能占总内能的比例一致。

### 4. 线速度

自转线速度与公转线速度：

$$v_{s1} = \omega R_1 \approx 3.011 \times 10^8\text{ m/s} \approx 1.005c$$
$$v_{s2} = \omega R_2 \approx 2.984 \times 10^8\text{ m/s} \approx 0.995c$$
$$v_{o1} = \omega R_1 = v_{s1}, \quad v_{o2} = \omega R_2 = v_{s2}$$

线速度在光速附近，与电子模型中 $v_s = c$ 的结果一致。盘 1 与盘 2 的线速度差异小于 1%。

### 5. 力平衡的数值验证

将上述参数代入稳定性方程，验证两盘的净向外加速度是否相等。

**盘 1**（取向右为正方向）：

$$F_{\text{net}1} = \rho_{d1} \omega^2 (4 - A_1 - \pi) R_1^3 h_1 \approx 52.69\text{ N}$$
$$a_1 = \frac{F_{\text{net}1}}{m_1} \approx 6.359 \times 10^{28}\text{ m/s}^2$$

**盘 2**（取向左为正方向）：

$$F_{\text{net}2} = \rho_{d2} \omega^2 (4 + A_2 - \pi) R_2^3 h_2 \approx 53.66\text{ N}$$
$$a_2 = \frac{F_{\text{net}2}}{m_2} \approx 6.359 \times 10^{28}\text{ m/s}^2$$

两盘净向外加速度相等（相对差异 $< 6 \times 10^{-8}$），验证了稳定性约束的自洽性。

以质子总质量计算的净向外力为：

$$F_{\text{net}} = a_1 m_p = a_2 m_p \approx 106.35\text{ N}$$

这个力虽然微小（相对于质子内能），但足以驱动质子绕切点产生缓慢进动。

力比值检查：

$$\frac{F_B}{F_M} = \frac{4}{A_p} \approx 1028, \quad \frac{F_C}{F_B} = \frac{\pi}{4} \approx 0.785$$

伯努利力远大于马格努斯力（约 1000 倍），这是因为质子密度远高于电子密度，导致近场耦合强度比值 $A_p$ 极小。离心力约为伯努利力的 78.5%。


### 6. 能量守恒验证

质子的总内能为：

$$E_{\text{tot}} = m_p c^2 \approx 1.503 \times 10^{-10}\text{ J}$$

**自转动能**：

$$E_{s1} = \frac{1}{2} I_{s1} \omega^2 \approx 3.758 \times 10^{-11}\text{ J}$$
$$E_{s2} = \frac{1}{2} I_{s2} \omega^2 \approx 3.758 \times 10^{-11}\text{ J}$$
$$E_{\text{self}} = E_{s1} + E_{s2} \approx 7.516 \times 10^{-11}\text{ J}$$

**公转动能**：

$$E_{o1} = \frac{1}{2} I_{o1} \omega^2 = E_{s1}$$
$$E_{o2} = \frac{1}{2} I_{o2} \omega^2 = E_{s2}$$
$$E_{\text{orbit}} = E_{o1} + E_{o2} = E_{\text{self}}$$

自转动能与公转动能完全相等。

**自转与公转动能之和**：

$$E_{\text{self}} + E_{\text{orbit}} \approx 1.503 \times 10^{-10}\text{ J}$$

与质子总内能的比值：

$$\frac{E_{\text{self}} + E_{\text{orbit}}}{m_p c^2} \approx 0.99990$$

剩余部分为进动动能：

$$E_{\text{prec}} = m_p c^2 - (E_{\text{self}} + E_{\text{orbit}}) \approx 1.544 \times 10^{-14}\text{ J}$$
$$\frac{E_{\text{prec}}}{m_p c^2} \approx 0.010\%$$

进动动能仅占总内能的约万分之一，为正数，物理上合理。这验证了 $\rho \propto 1/R^5$ 的密度分布假设是正确的——若假设两盘密度相等，则自转与公转动能之和将超过 $m_p c^2$，导致进动动能为负，这在物理上不可能。

### 7. Python 验证代码

读者可使用以下代码自行验证上述所有计算结果：

```python
import math

# ==================== 基本常数 ====================
h    = 6.62607015e-34        # J·s
hbar = h / (2 * math.pi)     # J·s
c    = 299792458.0           # m/s
me   = 9.1093837015e-31      # kg
mp   = 1.672621923e-27       # kg
k    = 0.302822              # 厚轴比

# ==================== 电子参数 ====================
Rs    = hbar / (me * c)
omega = me * c**2 / hbar
nu  = omega / (2 * math.pi)
rho_de = me / (2 * math.pi * k * Rs**3)

# ==================== 质子几何参数 ====================
x  = 1.009106                       # R1/R2 (第四节精确解)
R2 = (2 * Rs**3 / (x**3 + 1))**(1/3)
R1 = x * R2
h1 = k * R1
h2 = k * R2
V1 = math.pi * R1**2 * h1
V2 = math.pi * R2**2 * h2
Vp = V1 + V2

# ==================== 质量与密度 ====================
m1 = mp / (1 + x**2)
m2 = mp * x**2 / (1 + x**2)
rho_d1 = m1 / V1
rho_d2 = m2 / V2
rho_d_avg = mp / Vp

# ==================== 转动惯量 ====================
# 自转和公转的转动半径相同
Is1 = m1 * R1**2
Is2 = m2 * R2**2
Io1 = m1 * R1**2  # = Is1
Io2 = m2 * R2**2  # = Is2

# ==================== 能量 ====================
E_tot = mp * c**2
Es1   = 0.5 * Is1 * omega**2
Es2   = 0.5 * Is2 * omega**2
Eo1   = 0.5 * Io1 * omega**2  # = Es1
Eo2   = 0.5 * Io2 * omega**2  # = Es2
E_prec = E_tot - (Es1 + Es2 + Eo1 + Eo2)

# ==================== 角动量 ====================
Ls1 = Is1 * omega
Ls2 = Is2 * omega
Lo1 = Io1 * omega  # = Ls1
Lo2 = Io2 * omega  # = Ls2
L_rot   = Ls1 + Ls2 + Lo1 + Lo2
L_total = E_tot / (math.pi * nu)  # 由 E_rot = pi * L * nu
L_prec  = L_total - L_rot

# ==================== 线速度 ====================
vs1 = omega * R1
vs2 = omega * R2
vo1 = omega * R1  # = vs1
vo2 = omega * R2  # = vs2

# ==================== 力平衡 ====================
Ap   = (4 + math.pi) / 1836.15
A1   = Ap * rho_d_avg / rho_d1
A2   = Ap * rho_d_avg / rho_d2
LHS  = (4 - A1 - math.pi) * R1
RHS  = (4 + A2 - math.pi) * R2
F1   = rho_d1 * omega**2 * (4 - A1 - math.pi) * R1**3 * h1
F2   = rho_d2 * omega**2 * (4 + A2 - math.pi) * R2**3 * h2
a1   = F1 / m1
a2   = F2 / m2
Fnet = a1 * mp

# ==================== 输出 ====================
print(f"R1 = {R1:.6e} m, R2 = {R2:.6e} m")
print(f"R1/R2 = {x:.6f}")
print(f"Vp/Ve = {Vp/(2*math.pi*k*Rs**3):.10f}")
print(f"m1 = {m1:.6e} kg, m2 = {m2:.6e} kg")
print(f"rho_avg/rho_e = {rho_d_avg/rho_de:.4f}")
print(f"L_rot/hbar = {L_rot/hbar:.4f}")
print(f"L_total/hbar = {L_total/hbar:.4f}")
print(f"L_prec/hbar = {L_prec/hbar:.4f}")
print(f"vs1/c = {vs1/c:.6f}, vs2/c = {vs2/c:.6f}")
print(f"E_rot/E_tot = {(E_tot-E_prec)/E_tot:.8f}")
print(f"E_prec/E_tot = {E_prec/E_tot*100:.6f} %")
print(f"LHS/RHS (稳定性) = {LHS/RHS:.10f}")
print(f"a1/a2 (力平衡) = {a1/a2:.10f}")
print(f"Fnet = {Fnet:.10f}")
```

## 六、质子磁矩的推导（待完善）

### 1. 圆环模型的磁矩计算

在第三章中，我们通过圆电流微元积分严格推导了电子的磁矩。对于质子，其两盘异向旋转，需要分别计算自转磁矩和公转磁矩。

假设电荷集中在盘边缘（圆环模型），单盘自转磁矩为：
$$\mu_{s,i} = \frac{1}{2} q_i \omega R_i^2$$

单盘公转磁矩为：
$$\mu_{o,i} = \frac{1}{2} q_i \omega R_i^2$$

其中 $q_i = e \cdot V_i / V_p$ 为单盘电荷。

### 2. 自转磁矩与公转磁矩的抵消

两盘异向旋转，自转磁矩在系统中心相加：
$$\mu_{\text{self}} = \mu_{s,1} + \mu_{s,2} = \frac{1}{2} \omega (q_1 R_1^2 + q_2 R_2^2)$$

两盘同向公转，公转磁矩相加：
$$\mu_{\text{orbital}} = \mu_{o,1} + \mu_{o,2} = \frac{1}{2} \omega (q_1 R_1^2 + q_2 R_2^2)$$

比较两式可知：
$$\mu_{\text{self}} = \mu_{\text{orbital}}$$

在平衡状态下，自转磁矩与公转磁矩大小相等、方向相反，完全抵消，净磁矩为零。

### 3. 净向外力导致的自转轴偏移

然而，质子并非完全受力平衡的系统。由第二节的受力分析可知，两盘存在净向外力：

**盘 1**：
$$F_{\text{net},1} = F_{B1} - F_{M1} - F_{C1}$$

**盘 2**：
$$F_{\text{net},2} = F_{B2} + F_{M2} - F_{C2}$$

稳定性约束要求两盘的净向外加速度相等：
$$\frac{F_{\text{net},1}}{m_1} = \frac{F_{\text{net},2}}{m_2}$$

代入具体表达式并化简，可得加速度：
$$a_{\text{net}} = \frac{F_{\text{net},2}}{m_2} \approx 6.36 \times 10^{28}\text{ m/s}^2$$

对应的净向外力为：
$$F_{\text{net}} = m_p \cdot a_{\text{net}} \approx 106\text{ N}$$

这一净向外力没有平衡力与之抵消，它会导致质子的自转轴不能固定在两盘切点处，而是产生一个微小的偏移。这个偏移使得自转磁矩与公转磁矩不再严格反向，从而产生一个非零的净磁矩。

### 4. 进动与磁矩的产生

#### 物理图像

净向外力使质子处于非完全平衡态。两盘有分离趋势，但被伯努利吸引力约束在一起。这种约束不是刚性的——它允许两盘在径向发生微小位移。

在动态演化中，这种柔性约束导致系统有一个额外的自由度：两盘可以绕公转轴发生相对旋转。这个相对旋转表现为整个双盘系统绕公转轴的缓慢进动。

#### 从旋转参考系推导进动角速度

在随公转角速度 $\omega$ 绕 $z$ 轴旋转的参考系中分析。

**径向运动方程**：

设 $r$ 为两盘间距相对于平衡值的偏离量。径向运动满足：

$$\mu \ddot{r} - \mu \omega^2 r + k_{\text{eff}} r = F_{\text{net}}$$

其中 $\mu = \frac{m_1 m_2}{m_1 + m_2}$ 为约化质量，$k_{\text{eff}}$ 为伯努利吸引力的有效弹簧系数。

有效弹簧系数 $k_{\text{eff}}$ 表征伯努利吸引力对间距变化的响应强度。量级上，$k_{\text{eff}} \sim \frac{F_B}{R_{\text{char}}}$，其中 $R_{\text{char}}$ 为质子的特征尺度（$R_1$ 或 $R_2$ 量级）。需要说明的是，伯努利力在两盘分离时的精确衰减规律尚未从流体力学第一性原理严格推导，此处采用量级估算。其合理性将在数值验证中检验。

平衡位置 $r_{\text{eq}}$ 满足：

$$r_{\text{eq}} = \frac{F_{\text{net}}}{k_{\text{eff}} - \mu \omega^2}$$

**科里奥利耦合**：

在旋转参考系中，径向速度 $\dot{r}$ 会产生科里奥利力 $2\mu \omega \dot{r}$，方向垂直于径向和旋转轴。这个力在 $z$ 方向（垂直于公转平面）产生耦合。

$z$ 方向的运动方程为：

$$\mu \ddot{z} + 2\mu \omega \dot{r} + k_z z = 0$$

其中 $k_z$ 是 $z$ 方向的有效恢复力系数。

**力矩与进动**：

在稳态进动中，径向振荡振幅为 $r_{\text{eq}}$，频率为 $\omega$。因此 $\dot{r} \sim \omega r_{\text{eq}}$。

科里奥利力产生的力矩为：

$$\tau = 2\mu \omega \dot{r} \cdot (R_1 - R_2) \sim 2\mu \omega^2 r_{\text{eq}} (R_1 - R_2)$$

由角动量定理 $\tau = \omega_p L_{\text{orbital}}$，得到进动角速度：

$$\omega_p = \frac{2\mu \omega^2 r_{\text{eq}} (R_1 - R_2)}{L_{\text{orbital}}}$$

将 $r_{\text{eq}}$ 的表达式代入：

$$\omega_p = \frac{2\mu \omega^2 F_{\text{net}} (R_1 - R_2)}{(k_{\text{eff}} - \mu \omega^2) L_{\text{orbital}}}$$

#### 进动磁矩

进动角动量为：

$$L_p = I_{\text{orbital}} \cdot \omega_p$$

进动产生的等效磁矩为：

$$\mu_p = \frac{e}{4m_p} \cdot L_p$$

将上述关系联立，质子进动磁矩的表达式为：

$$\mu_p = \frac{e}{4m_p} \cdot I_{\text{orbital}} \cdot \frac{2\mu \omega^2 F_{\text{net}} (R_1 - R_2)}{(k_{\text{eff}} - \mu \omega^2) L_{\text{orbital}}}$$

### 5. 数值验证

代入具体参数：

约化质量 $\mu \approx 4.18 \times 10^{-28}\text{ kg}$，有效弹簧系数 $k_{\text{eff}} \approx 6.53 \times 10^{14}\text{ N/m}$：

平衡位置：
$$r_{\text{eq}} = \frac{F_{\text{net}}}{k_{\text{eff}} - \mu \omega^2} \approx 2.65 \times 10^{-13}\text{ m}$$

公转角动量：
$$L_{\text{orbital}} = m_1 \omega R_2^2 + m_2 \omega R_1^2 \approx 1.94 \times 10^{-31}\text{ J}\cdot\text{s}$$

进动角速度：
$$\omega_p = \frac{2\mu \omega^2 r_{\text{eq}} (R_1 - R_2)}{L_{\text{orbital}}} \approx 2.41 \times 10^{18}\text{ rad/s}$$

公转转动惯量：
$$I_{\text{orbital}} = m_1 R_2^2 + m_2 R_1^2 \approx 2.49 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$

进动角动量：
$$L_p = I_{\text{orbital}} \cdot \omega_p \approx 6.02 \times 10^{-34}\text{ J}\cdot\text{s}$$

进动磁矩：
$$\mu_p = \frac{e}{4m_p} \cdot L_p \approx 1.44 \times 10^{-26}\text{ J/T}$$

以核磁子为单位（$\mu_N \approx 5.051 \times 10^{-27}\text{ J/T}$）：
$$\frac{\mu_p}{\mu_N} \approx 2.85$$

实验值为：
$$\mu_p^{\text{exp}} \approx 2.793\mu_N$$

### 6. 差异分析与进一步研究

模型计算值（2.85 μN）与实验值（2.793 μN）相差约 2.2%，在经典流体模型的精度范围内已高度吻合。这一微小差异可能源于以下因素：

1. **进动几何的修正**：实际的进动轴与公转轴的夹角可能并非简单的 $90^\circ$，需要考虑进动锥面的几何投影。
2. **电荷分布的精细结构**：电荷可能并非完全集中在盘边缘，而是有一定的径向分布，这会影响有效磁矩。
3. **多体相互作用的屏蔽**：当质子与其他粒子（如电子或其他质子）结合时，外部场会部分屏蔽净向外力，从而减小有效磁矩。

尽管数值上存在差异，但模型给出了正确的定性图像：

1. 质子的磁矩远小于电子磁矩（约 $m_e/m_p$ 量级），这是因为两盘异向旋转导致磁矩大部分抵消。
2. 净磁矩来源于净向外力导致的自转轴偏移和非半径的不对称。
3. 若 $A_p \to 0$（即质子密度趋于无穷大），则净向外力趋于零，自转轴不再偏移，净磁矩趋于零。

这表明：质子的磁矩本质上是其内部流体结构非平衡性的体现，净向外力是质子与其他粒子相互作用（如形成氢分子或原子核）的根本原因。

*（本章推导进行中，待续）*
