# 第四章——质子

> ***"万物负阴而抱阳‌，冲气以为和"***

## 引言

在第三章中，我们确立了电子的同向双吸积盘模型，并定量推导了其全部核心物理参数。电子的两盘自转方向相同，马格努斯力向内抽吸，形成局域低压力势差场，表现为负电荷。

本章我们将研究质子。质子与电子携带等量异号电荷，质量约为电子的 1836.15 倍。在流体动力学框架下，质子的正电荷特性要求其内部流体势场向外发散，这与电子的向内汇聚势场形成鲜明对比。

我们将沿用相同的方法论：从实验数据出发，结合经典受力分析与能量耗散最小化原则，逆向推导质子的内部拓扑结构，并验证其自洽性。

## 一、线索收集与质子结构假设

### 1. 实验数据

质子的基本实验参数如下：

- 质子质量： $m_p \approx 1.672621923 \times 10^{-27}\text{ kg}$
- 质子内能： $E_{\text{rot},p} \approx 938.272\text{ MeV}$
- 质子电荷： $+e \approx 1.602176634 \times 10^{-19}\text{ C}$
- 质子磁矩： $\mu_p \approx 1.410606797 \times 10^{-26}\text{ J/T}$

质子质量约为电子质量的 1836.15 倍，内能约为电子内能的 1836.15 倍。这意味着质子的内部结构与电子有显著差异。

### 2. 正电荷的流体本原

在第三章第十七节中，我们已经指出：同向双吸积盘结构因马格努斯力向内抽吸，形成负电荷的汇聚势场；而异向双吸积盘结构因非对称加速度向外发散，形成正电荷的高压喷射场。

因此，质子的内部结构必须是**异向旋转**的双吸积盘。

### 3. 基本结构假设

基于上述分析，我们提出以下质子结构假设：

1. **异向旋转**：盘 1 自转顺时针，盘 2 自转逆时针。两盘自转轴平行于公转轴，系统整体逆时针公转。
2. **半径不同**：盘 1 半径较大（ $R_1$ ），盘 2 半径较小（ $R_2$ ）。
3. **公转轴位置**：位于两盘相切处。

异向旋转导致两盘的马格努斯力方向相同（均指向盘 1 的外侧），而伯努利力为吸引力（指向对方）。这种非对称的受力布局是质子产生净向外力的根源，也是其正电荷特性的力学本原。

## 二、频率相等的推导

在第三章第八节中，我们通过能量耗散最小化原则证明了同向双吸积盘结构中自转角速度与公转角速度严格同步。对于质子异向双吸积盘模型，这一结论同样成立。

### 1. 能量耗散最小化的约束

根据第一章第六节的稳态系统与能量耗散最小化原则，任何能够在绝对空间中长期稳定存在的物理实体，其内部运动必须满足零耗散条件（ $\dot{E}_{\text{dis}} \to 0$ ）。

考虑质子异向双吸积盘系统：
- **盘 1（左侧）**：自转角速度 $\omega_{s,1}$（顺时针），公转角速度 $\omega_o$（逆时针）
- **盘 2（右侧）**：自转角速度 $\omega_{s,2}$（逆时针），公转角速度 $\omega_o$（逆时针）

### 2. 盘体内部的相位锁定

对于每个吸积盘，其内部流体在随系统公转的同时进行自转。若自转角速度与公转角速度存在失配，则盘体在公转一周的过程中，其内部各点的相对相位会发生周期性变化。

具体而言，设盘 1 的自转角速度与公转角速度之差为 $\Delta\omega_1 = |\omega_{s,1}| - |\omega_o|$。在公转周期 $T_o = 2\pi/\omega_o$ 内，盘 1 内部某一点相对于盘体参考系的相位偏移为：

$$\Delta\phi_1 = \Delta\omega_1 T_o = 2\pi \left( \frac{|\omega_{s,1}|}{\omega_o} - 1 \right)$$

若 $\Delta\omega_1 \neq 0$，则每公转一周，该点的相对相位都会发生累积性漂移。这种周期性相位变化会在盘体内部激发粘滞剪切应力，导致能量持续耗散至背景介质。

同理，盘 2 的相位偏移为：

$$\Delta\phi_2 = \Delta\omega_2 T_o = 2\pi \left( \frac{|\omega_{s,2}|}{\omega_o} - 1 \right)$$

### 3. 界面处的剪切耗散

除了盘体内部的耗散外，两盘在相切界面处的流体微元也存在相对运动。在切点附近，盘 1 边缘的切向流速与盘 2 边缘的切向流速必须匹配，否则会产生界面剪切应力。

异向旋转时，两盘在切点处的自转线速度方向相同（均指向盘 1 的外侧）。设盘 1 在切点处的自转线速度为 $v_{s,1} = \omega_{s,1} R_1$，盘 2 在切点处的自转线速度为 $v_{s,2} = \omega_{s,2} R_2$。若两者不匹配，界面处将产生持续的粘性剪切耗散。

### 4. 稳态条件：频率同步

质子作为寿命极长的基本粒子（实验下限 $> 10^{34}$ 年），必然处于长期稳定状态。根据能量耗散最小化原则：

1. **盘 1 内部**：必须满足 $|\omega_{s,1}| = \omega_o$，否则内部相位漂移导致非稳态
2. **盘 2 内部**：必须满足 $|\omega_{s,2}| = \omega_o$，否则内部相位漂移导致非稳态
3. **两盘界面**：必须满足 $v_{s,1} = v_{s,2}$ 与几何自洽，否则界面剪切导致非稳态

因此，两盘的自转角速度均必须与公转角频率严格同步：

$$|\omega_{s,1}| = |\omega_{s,2}| = \omega_o \equiv \omega$$

即：**盘 1、盘 2 的自转角频率与系统整体的公转频率三者严格同步**。

### 5. 物理图像

这一结论并非先验假设，而是稳态系统动力学约束的必然结果：

| 情形                                                         | 能量耗散                              | 稳定性               |
| :----------------------------------------------------------- | :------------------------------------ | :------------------- |
| $\omega_{s,1} \neq \omega_o$ 或 $\omega_{s,2} \neq \omega_o$ | 内部相位漂移与界面剪切 → 持续能量泄漏 | 非稳态，无法长期存在 |
| $\omega_{s,1} = \omega_o$ 且 $\omega_{s,2} = \omega_o$       | 无相对运动 → 零耗散                   | **稳态，可长期存在** |

因此，频率相等是质子作为稳定粒子在绝对空间中维持其拓扑结构与旋转运动状态不变的必要条件。该同步性并非人为构造的假设，而是能量耗散最小化原则在稳态极限下的必然推论。

后续受力分析与定量计算均在这一频率同步的基础上展开。

## 三、受力分析

### 1. 力的方向判定

设盘 1 在左侧，盘 2 在右侧，公转轴位于两盘切点。系统整体逆时针公转。由第二节已证，两盘自转角速度与公转角速度相等（ $|\omega_{s,1}| = |\omega_{s,2}| = \omega_o \equiv \omega$ ）。

**盘 1（顺时针自转）**：
- 向心力：向右（指向公转轴）
- 马格努斯力：向左（远离公转轴，指向盘 1 的外侧）
- 伯努利力：向右（异向旋转时为吸引力，指向盘 2）

**盘 2（逆时针自转）**：
- 向心力：向左（指向公转轴）
- 马格努斯力：向左（指向公转轴）
- 伯努利力：向左（吸引力，指向盘 1）

注意：盘 1 与盘 2 的马格努斯力虽然均指向左侧，但相对于各自的公转半径，盘 1 的马格努斯力向外（远离公转轴），盘 2 的马格努斯力向内（指向公转轴）。这是因为两盘自转方向相反，与公转的相对关系不同。

### 2. 力的表达式（角速度形式）

沿用第三章第八节的力的表达式，用角速度表示。由第二节频率同步条件， $v_s = \omega R$， $v_o = \omega R$。

**盘 1**：

$$F_{M1} = k_{M,n} \rho_{0,n} \omega^2 R_1^3 h_1$$

$$F_{B1} = 4 \rho_{d1} \omega^2 R_1^3 h_1$$

$$F_{C1} = \pi \rho_{d1} \omega^2 R_1^3 h_1$$

**盘 2**：

$$F_{M2} = k_{M,n} \rho_{0,n} \omega^2 R_2^3 h_2$$

$$F_{B2} = 4 \rho_{d2} \omega^2 R_2^3 h_2$$

$$F_{C2} = \pi \rho_{d2} \omega^2 R_2^3 h_2$$

其中 $k_{M,n}$ 为近场马格努斯系数， $\rho_{0,n}$ 为近场背景介质密度。

### 3. 稳定性约束

对于盘 1，取向右为正方向（远离公转轴），净向外力为：

$$F_{net1} = F_{B1} - F_{M1} - F_{C1}$$

对于盘 2，取向左为正方向（远离公转轴），净向外力为：

$$F_{net2} = F_{B2} + F_{M2} - F_{C2}$$

两盘的质量分别为：

$$m_1 = \pi \rho_{d1} R_1^2 h_1, \quad m_2 = \pi \rho_{d2} R_2^2 h_2$$

稳定性约束要求两盘的净向外加速度相等，即：

$$\frac{F_{net1}}{m_1} = \frac{F_{net2}}{m_2}$$

代入具体表达式并约去公共因子 $\omega^2$：

$$\frac{(4\rho_{d1} - k_{M,n}\rho_{0,n} - \pi\rho_{d1}) R_1}{\pi\rho_{d1}} = \frac{(4\rho_{d2} + k_{M,n}\rho_{0,n} - \pi\rho_{d2}) R_2}{\pi\rho_{d2}}$$

整理分子，定义近场耦合强度与单盘密度之比 $A_1 = \frac{k_{M,n}\rho_{0,n}}{\rho_{d1}}$， $A_2 = \frac{k_{M,n}\rho_{0,n}}{\rho_{d2}}$，代入上式约去 $\pi$：

$$(4 - \pi - A_1) R_1 = (4 - \pi + A_2) R_2$$

此式即为质子异向双吸积盘模型的核心稳定性方程。其中 $A_1$ 与 $A_2$ 分别表征两盘的近场耦合强度与密度之比，在密度尚未确定相等之前，两者作为独立变量保留。

## 四、近场耦合参数与半径比值求解

### 1. 质子固有频率的确定

质子与电子可以稳定结合形成氢原子。根据第一章第六节的能量耗散最小化原则，若质子与电子的固有频率存在失配，则在原子尺度上会产生持续的周期性相位漂移，导致粘滞剪切耗散。只有当两者频率同步时，系统才能维持长期稳态。

因此，质子的固有频率与电子相同：

$$\nu_p = \nu_e = \nu$$

### 2. 电荷等量约束与体积关系

质子与电子携带等量电荷，即 $e_p = e_e = e$。由第三章第十七节的电荷对应关系：

$$e = \kappa_Q \cdot \nu V$$

其中 $V$ 为粒子总体积。对于电子， $V_e = 2\pi k R_s^3$。对于质子， $V_p = \pi k (R_1^3 + R_2^3)$。

将 $\nu_p = \nu_e = \nu$ 和 $e_p = e_e = e$ 代入电荷对应关系：

$$\kappa_Q \cdot \nu V_p = \kappa_Q \cdot \nu V_e \implies V_p = V_e$$

即：

$$\pi k (R_1^3 + R_2^3) = 2\pi k R_s^3 \implies R_1^3 + R_2^3 = 2 R_s^3$$

### 3. 近场耦合强度比值

近场耦合强度 $k_{M,n}\rho_{0,n}$ 为系统固有参数，不随粒子种类变化。电子的近场耦合强度比值为 $A_e = k_{M,n}\rho_{0,n}/\rho_{d,e} = 4 + \pi$。

质子平均密度 $\rho_d = m_p/V_p = m_p/V_e$，因此：

$$\frac{\rho_d}{\rho_{d,e}} = \frac{m_p}{m_e} \approx 1836.15$$

质子的近场耦合强度比值为：

$$A_p = \frac{k_{M,n}\rho_{0,n}}{\rho_d} = \frac{k_{M,n}\rho_{0,n}}{\rho_{d,e}} \cdot \frac{\rho_{d,e}}{\rho_d} = \frac{4 + \pi}{1836.15} \approx 0.003889$$

### 4. 质量与密度的缩放关系

根据能量耗散最小化原则，稳态系统内部各组成部分的能量趋于均分。对于质子异向双吸积盘模型，两盘的自转动能近似相等： $E_{s1} \approx E_{s2}$。

单盘自转动能为：
$$E_s = \frac{1}{2} I \omega^2 = \frac{1}{2} (m R^2) (2\pi\nu)^2 = 2\pi^2 m \nu^2 R^2$$

在频率相等（ $\nu_1 = \nu_2 = \nu$ ）且能量相等（ $E_{s1} = E_{s2}$ ）的条件下，可得：
$$m_1 R_1^2 = m_2 R_2^2 \implies \frac{m_1}{m_2} = \left(\frac{R_2}{R_1}\right)^2 = \frac{1}{x^2}$$

即质量与半径的平方成反比。

吸积盘的体积为 $V = \pi k R^3$，密度为 $\rho_d = m/V = m/(\pi k R^3)$。结合质量缩放关系：
$$\frac{\rho_{d1}}{\rho_{d2}} = \frac{m_1}{m_2} \cdot \left(\frac{R_2}{R_1}\right)^3 = \left(\frac{R_2}{R_1}\right)^2 \cdot \left(\frac{R_2}{R_1}\right)^3 = \left(\frac{R_2}{R_1}\right)^5 = \frac{1}{x^5}$$

即密度与半径的五次方成反比。

由质量守恒 $m_1 + m_2 = m_p$，结合 $m_1/m_2 = 1/x^2$，解得：
$$m_1 = \frac{m_p}{x^2 + 1}, \quad m_2 = \frac{m_p x^2}{x^2 + 1}$$

### 5. $A_1$、$A_2$ 与 $A_p$ 的关系

由定义 $A_i = k_{M,n}\rho_{0,n}/\rho_{di}$ 和 $\rho_d = m_p/(V_1 + V_2)$：

$$\frac{A_1}{A_p} = \frac{\rho_d}{\rho_{d1}} = \frac{m_p}{m_1} \cdot \frac{V_1}{V_1 + V_2} = (x^2 + 1) \cdot \frac{x^3}{x^3 + 1}$$

$$\frac{A_2}{A_p} = \frac{\rho_d}{\rho_{d2}} = \frac{m_p}{m_2} \cdot \frac{V_2}{V_1 + V_2} = \frac{x^2 + 1}{x^2} \cdot \frac{1}{x^3 + 1}$$

即：

$$A_1 = A_p \cdot \frac{x^3(x^2 + 1)}{x^3 + 1}, \quad A_2 = A_p \cdot \frac{x^2 + 1}{x^2(x^3 + 1)}$$

验证比值：

$$\frac{A_1}{A_2} = x^5 = \frac{\rho_{d2}}{\rho_{d1}}$$

### 6. 稳定性方程的数值求解

稳定性方程为：

$$(4 - A_1 - \pi)R_1 = (4 + A_2 - \pi)R_2$$

两边除以 $R_2$，代入 $A_1$、$A_2$ 的表达式。引入中间变量 $C = A_p \cdot \frac{x^2 + 1}{x^3 + 1}$，则 $A_1 = C x^3$，$A_2 = C/x^2$。代入并整理：

$$(4 - C x^3 - \pi)x = 4 + \frac{C}{x^2} - \pi$$

$$(4 - \pi)(x - 1) = C\left(x^4 + \frac{1}{x^2}\right) = A_p \cdot \frac{x^2 + 1}{x^3 + 1} \cdot \frac{x^6 + 1}{x^2}$$

由于 $A_p \approx 0.00389 \ll 1$，可预期 $x$ 非常接近 1。令 $x = 1 + \delta$（$\delta \ll 1$），零阶近似下：

$$(4 - \pi)\delta \approx A_p \cdot \frac{2}{2} \cdot 2 = 2A_p \implies \delta \approx \frac{2A_p}{4 - \pi} \approx 0.00906$$

通过牛顿迭代法数值求解，得到精确解：

$$x = \frac{R_1}{R_2} = 1.009106$$

**Python 验证代码**：读者可用以下脚本自行验证数值求解过程：

```python
import math

# 近场耦合强度比值
A_p = (4 + math.pi) / 1836.15

# 稳定性方程 f(x) = 0
def f(x):
    C = A_p * (x**2 + 1) / (x**3 + 1)
    lhs = (4 - C * x**3 - math.pi) * x
    rhs = 4 + C / x**2 - math.pi
    return lhs - rhs

# 牛顿迭代求解
x = 1.009  # 初始值（近似解）
for i in range(20):
    dx = 1e-10
    df = (f(x + dx) - f(x - dx)) / (2 * dx)
    x = x - f(x) / df

print(f"R1/R2 = {x:.10f}")
print(f"f(x)  = {f(x):.3e}")

# 验证稳定性方程
C = A_p * (x**2 + 1) / (x**3 + 1)
A1 = C * x**3
A2 = C / x**2
lhs = (4 - A1 - math.pi) * x
rhs = 4 + A2 - math.pi
print(f"LHS/RHS = {lhs/rhs:.15f}")
```

## 五、几何参数求解与自洽性验证

第四节已求出质子的核心几何与物理参数。本节直接引用这些结果，补充转动惯量与角动量的计算，并完成力平衡与能量守恒的数值验证。

### 1. 几何参数汇总

由第四节得到的半径比 $x = R_1/R_2 = 1.009106$，结合体积约束 $R_1^3 + R_2^3 = 2 R_s^3$，解得：


$$R_2 = \left(\frac{2 R_s^3}{x^3 + 1}\right)^{1/3} \approx 3.844 \times 10^{-13}\text{ m}$$

$$R_1 = x R_2 \approx 3.879 \times 10^{-13}\text{ m}$$

两盘半径均接近电子特征半径 $R_s \approx 3.862 \times 10^{-13}\text{ m}$。


两盘厚度（取厚轴比 $k = 0.302822$）：

$$h_1 = k R_1 \approx 1.175 \times 10^{-13}\text{ m}, \quad h_2 = k R_2 \approx 1.164 \times 10^{-13}\text{ m}$$

单盘体积：

$$V_1 = \pi R_1^2 h_1 \approx 5.553 \times 10^{-38}\text{ m}^3, \quad V_2 = \pi R_2^2 h_2 \approx 5.404 \times 10^{-38}\text{ m}^3$$

总体积 $V_p = V_1 + V_2 \approx 1.096 \times 10^{-37}\text{ m}^3$，与电子体积 $V_e$ 完全一致，验证了电荷等量约束的自洽性。

### 2. 质量分布

由第四节第4小节的缩放关系（频率相等时质量与半径平方成反比）：

$$\frac{m_1}{m_2} = \frac{R_2^2}{R_1^2} = \frac{1}{x^2} \approx 0.9820$$

结合 $m_1 + m_2 = m_p$，解得：
$$m_1 = \frac{m_p}{x^2 + 1} \approx 8.287 \times 10^{-28}\text{ kg}$$

$$m_2 = \frac{m_p x^2}{x^2 + 1} \approx 8.439 \times 10^{-28}\text{ kg}$$

两盘质量接近但不完全相等，差异源于半径的微小不对称。盘 2 半径较小但质量较大，这是因为密度与半径的五次方成反比（ $\rho_{d1}/\rho_{d2} = R_2^5/R_1^5 \approx 0.9557$ ），盘 2 的密度更高。

$$\rho_{d1} = \frac{m_1}{V_1} \approx 1.492 \times 10^{10}\text{ kg/m}^3$$

$$\rho_{d2} = \frac{m_2}{V_2} \approx 1.562 \times 10^{10}\text{ kg/m}^3$$

验证： $\rho_{d1}/\rho_{d2} \approx 0.9557$， $(R_2/R_1)^5 \approx 0.9557$，两者一致。

两盘的平均密度为：

$$\rho_{d,\text{avg}} = \frac{m_p}{V_p} \approx 1.527 \times 10^{10}\text{ kg/m}^3$$

该密度约为电子密度的 1836.15 倍，与质量比一致。

由于 $m_1 \neq m_2$，系统的质心不位于两盘切点。以切点为原点，盘 1 指向负方向，盘 2 指向正方向。盘 1 圆心在 $-R_1$，盘 2 圆心在 $+R_2$。质心位置为：

$$x_{\text{cm}} = \frac{m_1(-R_1) + m_2(R_2)}{m_1 + m_2}$$

代入数值：

$$x_{\text{cm}} = \frac{8.287 \times (-3.879) + 8.439 \times 3.844}{16.726} \times 10^{-13}\text{ m} \approx 1.75 \times 10^{-15}\text{ m}$$

定义质心偏移量 $\Delta x = |x_{\text{cm}}| \approx 1.75 \times 10^{-15}\text{ m}$。相对于盘半径：

$$\frac{\Delta x}{R_1} \approx 0.0045, \quad \frac{\Delta x}{R_2} \approx 0.0045$$

质心偏移仅约半径的 $0.45\%$，非常微小。但由于质子的自转磁矩与公转磁矩将发生高度对消，这一微小偏移将在磁矩计算中起到关键作用。

公转轴通过质心，两盘圆心到公转轴的距离分别为：

$$d_1 = R_1 + \Delta x \approx 3.897 \times 10^{-13}\text{ m}$$
$$d_2 = R_2 - \Delta x \approx 3.827 \times 10^{-13}\text{ m}$$

后续转动惯量与公转线速度的计算将使用 $d_1$、$d_2$ 而非 $R_1$、$R_2$。

### 3. 转动惯量与角动量

吸积盘的质量主要集中在边缘，转动惯量取 $I = m R^2$。

自转轴通过盘心，自转转动惯量为：

$$I_{s1} = m_1 R_1^2 \approx 1.247 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$
$$I_{s2} = m_2 R_2^2 \approx 1.247 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$

公转轴通过系统质心（而非几何切点）。盘 1 圆心到公转轴的距离为 $d_1 = R_1 + \Delta x$，盘 2 圆心到公转轴的距离为 $d_2 = R_2 - \Delta x$。其中 $\Delta x \approx 1.75 \times 10^{-15}\text{ m}$ 为质心偏移量（见第2小节）。公转转动惯量为：

$$I_{o1} = m_1 d_1^2 \approx 1.258 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$
$$I_{o2} = m_2 d_2^2 \approx 1.236 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$

由于 $\Delta x \ll R$， $I_{oi} \approx I_{si}$ 的近似仍然很好（相对差异约 $1\%$ ）。总转动惯量为：

$$I_{\text{self}} = I_{s1} + I_{s2} \approx 2.494 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$
$$I_{\text{orbit}} = I_{o1} + I_{o2} \approx 2.494 \times 10^{-52}\text{ kg}\cdot\text{m}^2$$

角速度为：

$$\omega = \omega_e = \frac{m_e c^2}{\hbar} \approx 7.763 \times 10^{20}\text{ rad/s}$$

各角动量分量：

$$L_{s1} = I_{s1} \omega \approx 9.681 \times 10^{-32}\text{ J}\cdot\text{s} \approx 917.98\hbar$$
$$L_{s2} = I_{s2} \omega \approx 9.681 \times 10^{-32}\text{ J}\cdot\text{s} \approx 917.98\hbar$$
$$L_{o1} = I_{o1} \omega \approx 9.768 \times 10^{-32}\text{ J}\cdot\text{s} \approx 926.28\hbar$$
$$L_{o2} = I_{o2} \omega \approx 9.593 \times 10^{-32}\text{ J}\cdot\text{s} \approx 909.64\hbar$$

自转角动量与公转角动量之和为：

$$L_{\text{rot}} = L_{\text{self}} + L_{\text{orbit}} \approx 3.872 \times 10^{-31}\text{ J}\cdot\text{s} \approx 3671.89\hbar$$

由第二章的公式 $E_{\text{rot}} = \pi L \nu$，质子的总角动量为：

$$L_{\text{total}} = \frac{E_{\text{tot}}}{\pi \nu} = \frac{m_p c^2}{\pi \nu} \approx 3.873 \times 10^{-31}\text{ J}\cdot\text{s} \approx 3672.31\hbar$$

两者的差值为进动角动量：

$$L_{\text{prec}} = L_{\text{total}} - L_{\text{rot}} \approx 4.36 \times 10^{-35}\text{ J}\cdot\text{s} \approx 0.41\hbar$$

### 4. 线速度

自转线速度：

$$v_{s1} = \omega R_1 \approx 3.011 \times 10^8\text{ m/s} \approx 1.005c$$
$$v_{s2} = \omega R_2 \approx 2.984 \times 10^8\text{ m/s} \approx 0.995c$$

公转线速度（绕质心）：

$$v_{o1} = \omega d_1 = \omega(R_1 + \Delta x) \approx 3.025 \times 10^8\text{ m/s} \approx 1.009c$$
$$v_{o2} = \omega d_2 = \omega(R_2 - \Delta x) \approx 2.970 \times 10^8\text{ m/s} \approx 0.991c$$

线速度在光速附近，与电子模型中 $v_s = c$ 的结果一致。盘 1 与盘 2 的线速度差异小于 1.5%。

### 5. 力平衡的数值验证

将上述参数代入稳定性方程，验证两盘的净向外加速度是否相等。

**盘 1**（取向右为正方向）：

$$F_{\text{net}1} = \rho_{d1} \omega^2 (4 - A_1 - \pi) R_1^3 h_1 \approx 52.69\text{ N}$$
$$a_1 = \frac{F_{\text{net}1}}{m_1} \approx 6.359 \times 10^{28}\text{ m/s}^2$$

**盘 2**（取向左为正方向）：

$$F_{\text{net}2} = \rho_{d2} \omega^2 (4 + A_2 - \pi) R_2^3 h_2 \approx 53.66\text{ N}$$
$$a_2 = \frac{F_{\text{net}2}}{m_2} \approx 6.359 \times 10^{28}\text{ m/s}^2$$

两盘净向外加速度相等（相对差异 $< 6 \times 10^{-8}$），验证了稳定性约束的自洽性。

以质子总质量计算的净向外力为：

$$F_{\text{net}} = a_1 m_p = a_2 m_p \approx 106.35\text{ N}$$

这个力虽然微小（相对于质子内能），但足以驱动质子绕切点产生缓慢进动。

力比值检查：

$$\frac{F_B}{F_M} = \frac{4}{A_p} \approx 1028, \quad \frac{F_C}{F_B} = \frac{\pi}{4} \approx 0.785$$

伯努利力远大于马格努斯力（约 1000 倍），这是因为质子密度远高于电子密度，导致近场耦合强度比值 $A_p$ 极小。离心力约为伯努利力的 78.5%。


### 6. 能量守恒验证

质子的总内能为：

$$E_{\text{tot}} = m_p c^2 \approx 1.503 \times 10^{-10}\text{ J}$$

**自转动能**：

$$E_{s1} = \frac{1}{2} I_{s1} \omega^2 \approx 3.758 \times 10^{-11}\text{ J}$$
$$E_{s2} = \frac{1}{2} I_{s2} \omega^2 \approx 3.758 \times 10^{-11}\text{ J}$$
$$E_{\text{self}} = E_{s1} + E_{s2} \approx 7.516 \times 10^{-11}\text{ J}$$

**公转动能**（使用 $d_1$、$d_2$ 而非 $R_1$、$R_2$）：

$$E_{o1} = \frac{1}{2} I_{o1} \omega^2 \approx 3.792 \times 10^{-11}\text{ J}$$
$$E_{o2} = \frac{1}{2} I_{o2} \omega^2 \approx 3.724 \times 10^{-11}\text{ J}$$
$$E_{\text{orbit}} = E_{o1} + E_{o2} \approx 7.515 \times 10^{-11}\text{ J}$$

**自转与公转动能之和**：

$$E_{\text{self}} + E_{\text{orbit}} \approx 1.503 \times 10^{-10}\text{ J}$$

与质子总内能的比值：

$$\frac{E_{\text{self}} + E_{\text{orbit}}}{m_p c^2} \approx 0.99989$$

剩余部分为进动动能：

$$E_{\text{prec}} = m_p c^2 - (E_{\text{self}} + E_{\text{orbit}}) \approx 1.698 \times 10^{-14}\text{ J}$$
$$\frac{E_{\text{prec}}}{m_p c^2} \approx 0.011\%$$

进动动能仅占总内能的约万分之一，为正数，物理上合理。

### 7. Python 验证代码

读者可使用以下代码自行验证上述所有计算结果：

```python
import math

# ==================== 基本常数 ====================
h    = 6.62607015e-34        # J·s
hbar = h / (2 * math.pi)     # J·s
c    = 299792458.0           # m/s
me   = 9.1093837015e-31      # kg
mp   = 1.672621923e-27       # kg
k    = 0.302822              # 厚轴比

# ==================== 电子参数 ====================
Rs    = hbar / (me * c)
omega = me * c**2 / hbar
nu  = omega / (2 * math.pi)
rho_de = me / (2 * math.pi * k * Rs**3)

# ==================== 质子几何参数 ====================
x  = 1.009106                       # R1/R2 (第四节精确解)
R2 = (2 * Rs**3 / (x**3 + 1))**(1/3)
R1 = x * R2
h1 = k * R1
h2 = k * R2
V1 = math.pi * R1**2 * h1
V2 = math.pi * R2**2 * h2
Vp = V1 + V2

# ==================== 质量与密度 ====================
m1 = mp / (1 + x**2)
m2 = mp * x**2 / (1 + x**2)
rho_d1 = m1 / V1
rho_d2 = m2 / V2
rho_d_avg = mp / Vp

# ==================== 质心偏移 ====================
x_cm   = (m1 * (-R1) + m2 * R2) / mp
Delta_x = abs(x_cm)
d1     = R1 + Delta_x
d2     = R2 - Delta_x

# ==================== 转动惯量 ====================
Is1 = m1 * R1**2
Is2 = m2 * R2**2
Io1 = m1 * d1**2   # 修正：用 d1
Io2 = m2 * d2**2   # 修正：用 d2

# ==================== 能量 ====================
E_tot = mp * c**2
Es1   = 0.5 * Is1 * omega**2
Es2   = 0.5 * Is2 * omega**2
Eo1   = 0.5 * Io1 * omega**2
Eo2   = 0.5 * Io2 * omega**2
E_prec = E_tot - (Es1 + Es2 + Eo1 + Eo2)

# ==================== 角动量 ====================
Ls1 = Is1 * omega
Ls2 = Is2 * omega
Lo1 = Io1 * omega
Lo2 = Io2 * omega
L_rot   = Ls1 + Ls2 + Lo1 + Lo2
L_total = E_tot / (math.pi * nu)  # 由 E_rot = pi * L * nu
L_prec  = L_total - L_rot

# ==================== 线速度 ====================
vs1 = omega * R1
vs2 = omega * R2
vo1 = omega * d1   # 修正：用 d1
vo2 = omega * d2   # 修正：用 d2

# ==================== 力平衡 ====================
Ap   = (4 + math.pi) / 1836.15
A1   = Ap * rho_d_avg / rho_d1
A2   = Ap * rho_d_avg / rho_d2
LHS  = (4 - A1 - math.pi) * R1
RHS  = (4 + A2 - math.pi) * R2
F1   = rho_d1 * omega**2 * (4 - A1 - math.pi) * R1**3 * h1
F2   = rho_d2 * omega**2 * (4 + A2 - math.pi) * R2**3 * h2
a1   = F1 / m1
a2   = F2 / m2
Fnet = a1 * mp

# ==================== 输出 ====================
print(f"R1 = {R1:.6e} m, R2 = {R2:.6e} m")
print(f"R1/R2 = {x:.6f}")
print(f"Vp/Ve = {Vp/(2*math.pi*k*Rs**3):.10f}")
print(f"m1 = {m1:.6e} kg, m2 = {m2:.6e} kg")
print(f"rho_avg/rho_e = {rho_d_avg/rho_de:.4f}")
print(f"L_rot/hbar = {L_rot/hbar:.4f}")
print(f"L_total/hbar = {L_total/hbar:.4f}")
print(f"L_prec/hbar = {L_prec/hbar:.4f}")
print(f"vs1/c = {vs1/c:.6f}, vs2/c = {vs2/c:.6f}")
print(f"E_rot/E_tot = {(E_tot-E_prec)/E_tot:.8f}")
print(f"E_prec/E_tot = {E_prec/E_tot*100:.6f} %")
print(f"LHS/RHS (稳定性) = {LHS/RHS:.10f}")
print(f"a1/a2 (力平衡) = {a1/a2:.10f}")
print(f"Fnet = {Fnet:.10f}")
```

## 六、质子磁矩的推导

在第五节中，我们完成了质子异向双吸积盘模型的几何参数求解与能量自洽性验证。本节将推导质子的磁矩，并与实验值进行对比。

### 1. 微分旋转下的单盘磁矩积分

在第三章中，我们采用电荷集中在盘边缘的薄圆环模型计算了电子磁矩。对于质子，自转磁矩与公转磁矩将发生高度对消（残差仅约 $10^{-3}$ 量级），薄圆环假设会丢失主导级修正项。因此，必须采用更精确的微分旋转模型。

根据第二章第六节，吸积盘内部的速度分布满足：

$$v(r) = v_s \left(\frac{r}{R}\right)^{-\alpha}, \quad 0 < \alpha < 1$$

其中 $\alpha \approx 0.5$。吸积盘的质量与电荷主要集中在边缘区域，内圈角速度显著高于外圈，不能简化为刚体旋转。

设单盘总电荷为 $q$，均匀分布于圆柱体积 $V = \pi R^2 h$ 中，电荷体密度 $\rho_q = q/(\pi R^2 h)$。磁矩定义为：

$$\mu = \frac{1}{2} \int r_{\perp} J_\theta \, dV = \frac{1}{2} \int r_{\perp} (\rho_q v(r)) \, dV$$

其中 $r_{\perp}$ 为流体微元到自转轴的垂直距离。

**自转磁矩**：自转轴通过盘心， $r_{\perp} = r$。取柱坐标， $dV = 2\pi r h \, dr$，代入速度分布：

$$\mu_{\text{self}} = \frac{1}{2} \rho_q \int_0^R r \cdot v_s \left(\frac{r}{R}\right)^{-\alpha} \cdot 2\pi r h \, dr = \pi h \rho_q v_s R^\alpha \int_0^R r^{2-\alpha} \, dr$$

积分得：

$$\int_0^R r^{2-\alpha} \, dr = \frac{R^{3-\alpha}}{3-\alpha}$$

代回 $\rho_q = q/(\pi R^2 h)$ 并化简：

$$\mu_{\text{self}} = \frac{q v_s R}{3-\alpha} = \frac{q \omega R^2}{3-\alpha}$$

当 $\alpha = 0.5$ 时：

$$\mu_{\text{self}} = \frac{q \omega R^2}{2.5} = 0.4 \, q \omega R^2$$

对比刚体旋转假设（ $\mu_{\text{rigid}} = 0.5 \, q \omega R^2$ ），微分旋转使单盘自转磁矩减小了 $20\%$。

**公转磁矩**：公转运动是刚体旋转（所有点以相同的角速度 $\omega$ 绕公转轴转动），但公转轴不在盘心。对于盘 1，公转轴到盘心的距离为 $d_1$；对于盘 2，距离为 $d_2$。

流体微元到公转轴的垂直距离为 $r_{\perp} = d + r\cos\phi$（ $r$ 为微元到盘心的距离， $\phi$ 为方位角）。公转线速度为 $v_{\text{orb}} = \omega r_{\perp}$。磁矩积分：

$$\mu_{\text{orb}} = \frac{1}{2} \rho_q \int r_{\perp} \cdot \omega r_{\perp} \, dV = \frac{1}{2} \rho_q \omega \int (d + r\cos\phi)^2 \, dV$$

展开被积函数：

$$(d + r\cos\phi)^2 = d^2 + 2dr\cos\phi + r^2\cos^2\phi$$

对 $\phi$ 积分时，交叉项 $\int_0^{2\pi} \cos\phi \, d\phi = 0$ 消失， $\int_0^{2\pi} \cos^2\phi \, d\phi = \pi$。代入 $dV = r \, dr \, d\phi \, h$：

$$\mu_{\text{orb}} = \frac{1}{2} \rho_q \omega h \int_0^R \left( 2\pi d^2 r + \pi r^3 \right) dr = \pi \rho_q \omega h \left( d^2 \frac{R^2}{2} + \frac{R^4}{4} \right)$$

代回 $\rho_q = q/(\pi R^2 h)$：

$$\mu_{\text{orb}} = \frac{q \omega}{2} \left( d^2 + \frac{R^2}{4} \right)$$

这里的 $R^2/4$ 项源于吸积盘的有限厚度（公转轴不在盘心时，盘内各微元到公转轴的距离不同），是薄圆环模型所忽略的修正项。

### 2. 几何残差

由第五节第2小节，质心偏移量 $\Delta x \approx 1.75 \times 10^{-15}\text{ m}$，公转轴通过质心，两盘圆心到公转轴的距离为 $d_1 = R_1 + \Delta x$， $d_2 = R_2 - \Delta x$。

电荷按体积比例分配：

$$q_1 = e \cdot \frac{V_1}{V_p} \approx 0.5068e, \quad q_2 = e \cdot \frac{V_2}{V_p} \approx 0.4932e$$

**自转磁矩**（两盘异向旋转，自转磁矩方向相同，相加）：

$$\mu_{\text{self}} = \frac{\omega}{3-\alpha} \left( q_1 R_1^2 + q_2 R_2^2 \right)$$

**公转磁矩**（两盘同向公转，相加）：

$$\mu_{\text{orbital}} = \frac{\omega}{3-\alpha} \left( q_1 d_1^2 + q_2 d_2^2 \right)$$

代入数值（ $\alpha = 0.5$， $\omega \approx 7.763 \times 10^{20}\text{ rad/s}$ ）：

$$\mu_{\text{self}} \approx 1469.07\,\mu_N$$
$$\mu_{\text{orbital}} \approx 1469.34\,\mu_N$$

两者之差为几何残差：

$$\mu_{\text{net}} = \mu_{\text{orbital}} - \mu_{\text{self}} \approx 0.272\,\mu_N$$

自转磁矩与公转磁矩对消了 $99.98\%$ 以上，仅剩万分之几的残差。这一残差完全由质心偏移 $\Delta x$ 引起。

将 $d_1 = R_1 + \Delta x$ 和 $d_2 = R_2 - \Delta x$ 代入，展开到 $\Delta x$ 的一阶：

$$d_1^2 - R_1^2 \approx 2R_1\Delta x, \quad d_2^2 - R_2^2 \approx -2R_2\Delta x$$

几何残差可近似为：

$$\mu_{\text{net}} \approx \frac{\omega \Delta x}{3-\alpha} \cdot 2(q_1 R_1 - q_2 R_2)$$

由于 $q_1 R_1 \approx q_2 R_2$（电荷与半径均接近对称），这一残差非常小。

### 3. 进动分析

净向外力 $F_{\text{net}} \approx 106.35\text{ N}$ 沿径向作用于系统。由于质心偏离几何切点（偏移量 $\Delta x \approx 1.75 \times 10^{-15}\text{ m}$ ），该力对公转轴产生力矩：

$$\tau = F_{\text{net}} \cdot \Delta x \approx 1.86 \times 10^{-13}\text{ N}\cdot\text{m}$$

根据角动量定理，力矩导致系统角动量方向发生改变，表现为绕垂直于公转平面的轴缓慢进动。进动角速度为：

$$\omega_p = \frac{\tau}{L_{\text{total}}}$$

其中 $L_{\text{total}} \approx 3.873 \times 10^{-31}\text{ J}\cdot\text{s}$ 为质子总角动量。代入数值：

$$\omega_p \approx 4.81 \times 10^{17}\text{ rad/s}$$

与主旋转角速度 $\omega \approx 7.76 \times 10^{20}\text{ rad/s}$ 相比：

$$\frac{\omega_p}{\omega} \approx 6.19 \times 10^{-4}$$

进动角速度仅为主旋转的约万分之六，满足慢进动近似。

进动使总角动量矢量产生一个水平分量（垂直于公转轴）：

$$L_h = L_{\text{total}} \cdot \frac{\omega_p}{\omega} \approx 2.398 \times 10^{-34}\text{ J}\cdot\text{s} \approx 2.27\hbar$$

该水平角动量对应一个等效磁矩。由第三章的荷质比关系 $\gamma = e/(2m_p)$：

$$\mu_{\text{prec}} = \gamma \cdot L_h = \frac{e}{2m_p} \cdot L_h$$

代入数值：

$$\mu_{\text{prec}} \approx 1.148 \times 10^{-26}\text{ J/T} \approx 2.27\,\mu_N$$

### 4. 总磁矩与实验值对比

质子的总磁矩为几何残差与进动磁矩之和：

$$\mu_p = \mu_{\text{net}} + \mu_{\text{prec}}$$

代入数值：

$$\mu_p \approx 0.272\,\mu_N + 2.274\,\mu_N = 2.546\,\mu_N$$

实验值为：

$$\mu_p^{\text{exp}} \approx 2.793\,\mu_N$$

相对差异约 $8.8\%$。

**差异来源分析**：上述计算尚未计入吸积盘厚度对公转磁矩残差的修正。由于公转轴不在盘心，盘内各微元到公转轴的距离在 $z$ 方向（垂直于盘面）存在展宽。在 $d_{\perp}^2 = d^2 + r^2\cos^2\phi + z^2$ 的展开中， $z^2$ 项与 $\Delta x$ 的交叉项在微分旋转下不再完全对称抵消，而是与厚轴比 $k$ 发生流体动力学耦合，形成有效放大因子：

$$\mathcal{A}(\alpha, k) \approx 1 + \frac{k^2}{2} \cdot \frac{\Delta x}{R_s} \cdot \frac{1}{3-\alpha}$$

当 $\alpha = 0.5$、 $k = 0.302822$ 时：

$$\mathcal{A} \approx 1 + \frac{0.3028^2}{2} \cdot 0.0045 \cdot \frac{1}{2.5} \approx 1.000034$$

这一修正极小，不足以弥合 $8.8\%$ 的差距。更合理的解释是：进动角动量的水平分量在计算中采用了 $\gamma = e/(2m_p)$ 的刚体荷质比，而实际吸积盘的电荷分布与质量分布并不完全一致，有效荷质比应略高于刚体值。

定义有效荷质比修正因子 $\xi$：

$$\mu_p = \mu_{\text{net}} + \xi \cdot \mu_{\text{prec}}$$

反解 $\xi$：

$$\xi = \frac{2.793 - 0.272}{2.274} \approx 1.11$$

即有效荷质比约为刚体值的 $1.11$ 倍。这与吸积盘电荷集中于边缘（质量也集中于边缘，但电荷分布可能略偏外侧）的物理图像一致。

### 5. Python 验证代码

读者可使用以下代码自行验证上述磁矩计算的各中间步骤：

```python
import math

# ==================== 基本常数 ====================
h    = 6.62607015e-34
hbar = h / (2 * math.pi)
c    = 299792458.0
me   = 9.1093837015e-31
mp   = 1.672621923e-27
e    = 1.602176634e-19
k    = 0.302822
alpha = 0.5

# ==================== 质子参数 ====================
Rs    = hbar / (me * c)
omega = me * c**2 / hbar
nu    = omega / (2 * math.pi)

x  = 1.009106
R2 = (2 * Rs**3 / (x**3 + 1))**(1/3)
R1 = x * R2
h1 = k * R1
h2 = k * R2
V1 = math.pi * R1**2 * h1
V2 = math.pi * R2**2 * h2
Vp = V1 + V2

m1 = mp / (1 + x**2)
m2 = mp * x**2 / (1 + x**2)
rho_d1 = m1 / V1
rho_d2 = m2 / V2
rho_d_avg = mp / Vp

# 力平衡
Ap   = (4 + math.pi) / 1836.15
A1   = Ap * rho_d_avg / rho_d1
A2   = Ap * rho_d_avg / rho_d2
F1   = rho_d1 * omega**2 * (4 - A1 - math.pi) * R1**3 * h1
a1   = F1 / m1
Fnet = a1 * mp

# 电荷与质心偏移
q1 = e * V1 / Vp
q2 = e * V2 / Vp
x_cm = (m1 * (-R1) + m2 * R2) / mp
Delta_x = abs(x_cm)
d1 = R1 + Delta_x
d2 = R2 - Delta_x

# 角动量
L_total = mp * c**2 / (math.pi * nu)

mu_N = e * hbar / (2 * mp)

# ==================== 磁矩计算 ====================
# 自转磁矩 (微分旋转)
mu_self = omega * (q1 * R1**2 + q2 * R2**2) / (3 - alpha)

# 公转磁矩 (含厚度修正 R^2/4)
mu_orbital = omega * (q1 * (d1**2 + R1**2/4) + q2 * (d2**2 + R2**2/4)) / 2

# 几何残差
mu_net = mu_orbital - mu_self

# 进动
tau = Fnet * Delta_x
omega_p = tau / L_total
L_h = L_total * (omega_p / omega)
gamma = e / (2 * mp)
mu_prec = gamma * L_h

# 总磁矩
mu_total = mu_net + mu_prec

# ==================== 输出 ====================
print(f"mu_self/mu_N    = {mu_self/mu_N:.4f}")
print(f"mu_orb/mu_N     = {mu_orbital/mu_N:.4f}")
print(f"mu_net/mu_N     = {mu_net/mu_N:.6f}")
print(f"omega_p/omega   = {omega_p/omega:.6e}")
print(f"mu_prec/mu_N    = {mu_prec/mu_N:.4f}")
print(f"mu_total/mu_N   = {mu_total/mu_N:.6f}")
print(f"mu_exp/mu_N     = 2.792847")
print(f"相对差异        = {(mu_total/mu_N - 2.792847)/2.792847*100:.2f} %")
